# ProjectTwinFalls

Scaffold per ProjectTwinFalls (TwinFalls namespace) — offline-first personal finance app.

Run `dotnet restore` and `dotnet build` from solution root after adding a proper `.sln` (or run `dotnet new sln -n ProjectTwinFalls` and `dotnet sln add` for the projects).
